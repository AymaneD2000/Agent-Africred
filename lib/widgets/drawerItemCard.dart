import 'package:flutter/material.dart';

class Draweritemcard extends StatelessWidget {
  const Draweritemcard({super.key});

  @override
  Widget build(BuildContext context) {
    return Row();
  }
}